{# Extendemos la plantilla zonacorredores #}
{% extends 'AppCorredoresRiojaBundle:views:zonacorredores.html.twig' %}

{% block content %}

    <div class="contenido">
        
{% for mensaje in app.session.flashbag.get('info') %}
   	<p clas="info">{{ mensaje }}</p>
{% endfor %}        
                
<h1>Bienvenido a Carreras por La Rioja </h1>
 <p>Carreras por La Rioja es un sitio web donde corredores de todos los niveles pueden inscribirse en carreras organizadas en La Rioja. 
     A continuación se muestran algunas de las carreras a las que te puedes inscribir.</p>
        <div class="centro"/>
        <ul>
            <li class="columna">
                <ul>
                    {% for carrera in carrerasFuturas %}

                        <li class="celda">
                            <div class="parteCelda">
                                <img src="/bundles/appcorredoresrioja/images/matutrail.jpg"/>
                            </div>
                            <div class="parteCelda2">
                                <h3><a href="{{ path('app_corredores_rioja_carrera_por_slug', {'slug': carrera.nombre}, {'_locale': app.request.locale})}}">{{ carrera.nombre }}</a></h3>
                                <p>{{ carrera.fechaCelebracion|localizeddate('medium', 'short') }}</p>
                                <div style="float:right; padding:10px 5px">
                                    <a style="padding:5px 8px 5px 10px;background:#5C7E6D;color:#FFF" 
                                       href="{{ path('app_corredores_rioja_inscribir_carrera_por_slug', {'slug': carrera.nombre}, {'_locale': app.request.locale})}}"><span>Inscríbete</span> </a>
                                </div>
                            </div>    
                        </li>
                    {% endfor %}
                </ul>
            </li>
        </ul>
    </div>
        
        
{% endblock %}
